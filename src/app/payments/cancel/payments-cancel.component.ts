import { Component, ElementRef, EventEmitter } from '@angular/core';

@Component({
  selector: 'payments-cancel',
  template: require('./payments-cancel.html')
})
export class PaymentsCancelComponent {
  constructor() { }
}